<div class="row">
    {{link-to 'New task' 'tasks.new' class="btn btn-primary"}}
    <h1>Tasks</h1>

    <ul>
        {{#each task in controller}}
            <li>{{#link-to 'task' task}}{{task.title}}{{/link-to}}</li>
        {{/each}}
    </ul>

    {{outlet}}
</div>